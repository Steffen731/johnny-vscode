; =========================================================
; Aufgabe 16:
; Vergleich durch Dekrementieren
;
; Ergebnis:
;
; 0 -> gleich
; 1 -> A größer
; 2 -> B größer
; =========================================================


; ---------------------------------------------------------
; Ergebnis zunächst auf 0 setzen
; ---------------------------------------------------------

        NULL ergebnis


; ---------------------------------------------------------
; Arbeitskopien erzeugen
; ---------------------------------------------------------

        TAKE zahlA
        SAVE restA

        TAKE zahlB
        SAVE restB


; =========================================================
; Hauptschleife
; =========================================================

loop:

; ---------------------------------------------------------
; Ist A bereits 0?
; ---------------------------------------------------------

        TST restA
                        ; Falls A = 0:
                        ; nächste Zeile überspringen

        JMP pruefeB


; ---------------------------------------------------------
; A ist 0
; Prüfen:
; Ist B ebenfalls 0?
; ---------------------------------------------------------

aNull:

        TST restB
                        ; Falls B = 0:
                        ; nächste Zeile überspringen

        JMP bGroesser


; ---------------------------------------------------------
; Beide 0
; -> gleich
; ---------------------------------------------------------

        HLT 000


; =========================================================
; Prüfen:
; Ist B bereits 0?
; =========================================================

pruefeB:

        TST restB
                        ; Falls B = 0:
                        ; nächste Zeile überspringen

        JMP weiter


; ---------------------------------------------------------
; B zuerst 0
; -> A größer
; ---------------------------------------------------------

        JMP aGroesser


; =========================================================
; Beide weiter herunterzählen
; =========================================================

weiter:

        DEC restA

        DEC restB

        JMP loop


; =========================================================
; A größer
; Ergebnis = 1
; =========================================================

aGroesser:

        INC ergebnis

        HLT 000


; =========================================================
; B größer
; Ergebnis = 2
; =========================================================

bGroesser:

        INC ergebnis
        INC ergebnis

        HLT 000


; ---------------------------------------------------------
; Datenbereich
; ---------------------------------------------------------

zahlA:      DAT 5

zahlB:      DAT 6

restA:      DAT 0

restB:      DAT 0

ergebnis:   DAT 0